classes:
  application: "mudforge.gate.app.MudGate"

processors:
  raw: "mudforge.gate.processors.raw.RawProcessor"
  circle: "mudforge.gate.processors.circle.CircleProcessor"
  picklerich: "mudforge.gate.processors.picklerich.PickleRichProcessor"
